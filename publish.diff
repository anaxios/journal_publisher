--- publish-bak	2026-06-14 12:52:56
+++ publish	2026-06-14 12:51:36
@@ -1,11 +1,10 @@
 #!/usr/bin/env bash
-set -e
 
 #TODAY="$(date "+%F")"
-WORKING_DIR="$HOME/Projects/journal_publisher"
+#WORKING_DIR="$HOME/Projects/journal_publisher"
+WORKING_DIR="$(pwd)"
 OUTPUT_DIR="$WORKING_DIR/_site"
 LOCATION="$WTTR_LOCATION"
-FILE_NAME="$(today).md"
 
 pushd "$WORKING_DIR"
 
@@ -31,17 +30,6 @@
 source ./partials/header.sh
 source ./partials/footer.sh
 
-preflight() {
-    local prereqs=( "cowsay" "figlet" "lolcat" "aha" )
-    for req in "${prereqs[@]}"; do
-        if ! command -v "$req" > /dev/null 2>&1; then
-            echo "Requires $req to be installed."
-            exit 1
-        fi
-    done
-}
-preflight
-
 title() {
     printf '%s\n' "<h1>$*</h1>"
 }
@@ -107,44 +95,42 @@
     printf '%s' "</span>"
 }
 
-parse_date() {
-    case "$OSTYPE" in
-        darwin*) date -j -f "%Y-%m-%d" "$1" +"%a, %d %b %Y" ;;
-        linux*)  date -d "$1" +"%a, %d %b %Y" ;;
-        msys*)   echo "Windows?"; exit 1 ;;
-        *)       echo "Cannot parse dates"; exit 1 ;;
-    esac
-}
-
 entries=($(nb list interstitial_journal/ | grep --color=never -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' | sort -r))
 
 #make index.html
 (
     header
+    printf '%s\n' "<div class='flow'>"
     title_figlet "EVIL BOBS DIARY"
-    div "<pre class="ascii-banner">" "$(echo "This is my Interstitial Journal. It's my thoughts and useful things I want to remember." | comment-banner)" "</pre>"
+    printf '%s\n' "<pre class="ascii-banner">" \
+        "$(echo "This is my Interstitial Journal. It's my thoughts and useful things I want to remember." | comment-banner)" \
+        "</pre>"
     #div "$(paragraph "Entries:" "$(ul "$(for entry in "${entries[@]}"; do list_item "$(href "$entry")"; done)")")"
 
     for entry in "${entries[@]}"; do
-        TODAY="${entry}"
+        TODAY="$entry"
         #if ! [[ -a "$OUTPUT_DIR" ]];then mkdir "$OUTPUT_DIR"; fi
-        title_figlet "$(parse_date "${TODAY}" -R | awk '{print $1, $2, $3, $4}')"
+        printf '%s\n' "<div>"
+        title_figlet "$(date -d "$TODAY" -R | awk '{print $1, $2, $3, $4}')"
         moon_phase_html
         href "$entry"
         paragraph "$(nb export pandoc interstitial_journal/${entry}.md --from markdown --to html)"
+        printf '%s\n' "</div>"
     done
 
     kilroysay "Kilroy was here"
+    printf '%s\n' "</div>"
+    printf '%s\n' "<script src="./oneko.js"></script>"
     footer
 ) > "${OUTPUT_DIR}/index.html"
 
 # make journal entry pages
 for entry in "${entries[@]}"; do
-    TODAY="${entry}"
+    TODAY="$entry"
     if ! [[ -a "$OUTPUT_DIR" ]];then mkdir "$OUTPUT_DIR"; fi
     (
         header
-        title_figlet "$(parse_date "${TODAY}" -R | awk '{print $1, $2, $3, $4}')"
+        title_figlet "$(date -d "$TODAY" -R | awk '{print $1, $2, $3, $4}')"
         moon_phase_html
         paragraph "$(nb export pandoc interstitial_journal/${entry}.md --from markdown --to html)"
         footer
